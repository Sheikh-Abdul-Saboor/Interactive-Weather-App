This project is a simple yet functional weather application built using HTML, CSS, and JavaScript with data fetched from the OpenWeatherMap API.

🔹 Features

Search weather by city name.

Displays temperature, weather condition, humidity, and wind speed.

Dynamic weather icons (sunny, cloudy, rainy, etc.).

Responsive design that works on desktop and mobile.

Clean UI with styled weather cards.

🔹 Technologies Used

HTML5 → for structuring the app.

CSS3 → for styling and responsive layout.

JavaScript (ES6+) → for fetching and displaying weather data.

OpenWeatherMap API → to get real-time weather data.

🔹 How It Works

User enters a city name in the input field.

The app makes an API call to OpenWeatherMap with the city name.

Weather data is returned in JSON format.

The data (temperature, condition, humidity, wind speed) is displayed on the screen.

🔹 Future Enhancements

Show 5-day forecast instead of just current weather.

Add geolocation support to automatically detect user’s location.

Include background images that change based on weather conditions.# Interactive-Weather-App
